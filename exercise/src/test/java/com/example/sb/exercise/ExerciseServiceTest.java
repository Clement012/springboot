package com.example.sb.exercise;

import static org.hamcrest.MatcherAssert.assertThat;
import static org.hamcrest.Matchers.is;
import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import com.example.sb.exercise.service.ExerciseService;

@SpringBootTest
public class ExerciseServiceTest {
  
  //Autowired
  //private ExerciseService exerciseService;

  //Test
  /*void calculateTest(){  // Expected object source code//com.example.sb.exercise.dto.Calculation@19fbc594
    assertEquals(exerciseService.calculate("12", "11", "add"), is("23"));
    assertEquals(exerciseService.calculate("-8", "7", "add"), is("-1"));
    assertEquals(exerciseService.calculate("0", "0", "add"), is("0")); } */
  }

